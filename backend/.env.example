# ── Serveur ───────────────────────────────────────────────────────────────────
PORT=3001
ADMIN_TOKEN=change-me-secret-token

# ── Base de données (PostgreSQL - fourni automatiquement par Railway) ──────────
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── LLM Provider ─────────────────────────────────────────────────────────────
# Valeurs possibles : claude | openai | gemini | deepseek
LLM_PROVIDER=claude
# LLM_MODEL=claude-sonnet-4-6   # Optionnel - utilise le modèle par défaut du provider

# ── Clés API (au moins une requise selon le provider choisi) ──────────────────
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
# DEEPSEEK_API_KEY=...

# ── Source RSS ────────────────────────────────────────────────────────────────
RSS_FEED_URL=https://simpleflying.com/feed/

# ── Planificateur cron (optionnel, défaut : toutes les 6 heures) ──────────────
# CRON_SCHEDULE=0 */6 * * *
